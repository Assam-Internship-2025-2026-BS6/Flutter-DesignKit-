import 'package:flutter/material.dart';
 
/// Centralized color palette for the design kit.
/// 
/// Contains all brand colors, neutrals, and status-specific colors 
/// used throughout the application components.
class AppColors {
  /// The standard primary blue.
  static const primaryBlue = Color(0xFF1E40AF);

  /// A lighter variation of brand blue.
  static const lightBlue = Color(0xFF6FA8FF);

  /// A deep, dark blue primarily used for footer or secondary backgrounds.
  static const darkBlue = Color(0xFF0B1F5E);

  /// The official HDFC brand blue color.
  static const hdfcBlue = Color(0xFF004C8F); 

  /// A vibrant accent blue for active states or highlights.
  static const accentBlue = Color(0xFF5371F9);

  /// Pure white.
  static const white = Color(0xFFFFFFFF);

  /// Pure black.
  static const black = Color(0xFF000000);

  /// Neutral grey for borders or disabled states.
  static const grey = Color(0xFFD9D9D9);
}
